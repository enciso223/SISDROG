/**
 * Configuración global de la aplicación SISDROG Desktop.
 */

export const API_BASE_URL = 'http://localhost:8000';

export const API_TIMEOUT = 10000;

export const TAX_RATE = 0;

/**
 * Modo demo: cuando es true, los controladores usan datos mock
 * en lugar de llamar al backend. Útil para maquetar la interfaz.
 */
export const DEMO_MODE = false;

export const APP_NAME = 'SISDROG';
export const APP_VERSION = '1.0.0';
